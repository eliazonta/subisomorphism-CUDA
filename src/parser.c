#include <stdio.h>

int main(int argc, char** argv)
{
    FILE* f;
    f = fopen("examples/input.txt");


    while (f != NULL)
    {
        
    }
    int rowptr;
    int col;

    for (size_t i = 0; i < count; ++i)
    {
        for (size_t j = 0; j < count; ++j)
        {
            
        }
        
    }
    
    
    return 0;
}